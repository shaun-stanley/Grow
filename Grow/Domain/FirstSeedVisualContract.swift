import CoreGraphics

enum FirstSeedVisualContract {
    static let primaryActionHeight: CGFloat = 52
    static let optionMinHeight: CGFloat = 64
    static let secondaryActionMinHeight: CGFloat = 44
    static let outerSpacing: CGFloat = 24
    static let launchCropCount = 3
    static let launchSetupCount = 3

    static let antiSlopChecklist = [
        "Apple native system typography only",
        "One primary action per beat",
        "Specimen-first composition",
        "No nested card stacks",
        "Bloom appears only after earned success",
        "Selection uses glyph and label",
        "Sample mode never persists"
    ]
}
